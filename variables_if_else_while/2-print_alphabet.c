#include <stdio.h>
/**
 * main - Entry point. Determines if a random number is positive, negative, or zero
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char l = 'a';
	while (l <= 'z')
	{
		putchar(l);
		l++;
	}
	putchar('\n');
	return 0;
}
