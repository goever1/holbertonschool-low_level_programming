#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _atoi - Change the string to the number that the string contains
 * @s: It is poiting the string
 */
int _atoi(char *s)
{
	int n = atoi(s);
	return (0);
}
