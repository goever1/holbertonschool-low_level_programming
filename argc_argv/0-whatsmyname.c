#include <stdio.h>
/**
 * main  - prints it name follow by a \n
 * @argc: number of arguments
 * @argv: it is pointing to arguments
 * Return: IT return 0
 */
int main (int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return(0);
}
